{
  "name": "@agent-os/agent-monitor",
  "version": "0.1.0",
  "description": "Infrastructure monitoring agent for AgentOS",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "@agent-os/sdk": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.5.1",
    "tsx": "^4.19.0",
    "typescript": "^5.9.3"
  }
}
