# ═══════════════════════════════════════════════════════════════
# AgentKernel — Security Sandbox Configuration
# Copy to .env and fill in your values: cp .env.example .env
# ═══════════════════════════════════════════════════════════════

NODE_ENV=development

# ─── INFRASTRUCTURE ───────────────────────────────────────────
# PostgreSQL for audit logs and state persistence
DATABASE_URL=postgresql://agentkernel:agentkernel@localhost:5432/agentkernel

# Redis for event bus
REDIS_URL=redis://localhost:6379

# Qdrant for vector storage (optional)
QDRANT_URL=http://localhost:6333

# ─── SECURITY & PERMISSIONS ──────────────────────────────────
# PERMISSION_SECRET signs capability tokens (HMAC-SHA256).
# Use a long random string (32+ characters).
PERMISSION_SECRET=change-me-to-a-long-secret-string-32-chars

# ─── PRODUCTION HARDENING ────────────────────────────────────
# When enabled, AgentKernel will refuse to start with insecure defaults.
# ENFORCE_PRODUCTION_HARDENING=true

# ─── OPENCLAW PROXY ──────────────────────────────────────────
# Port for the security proxy (clients connect here)
OPENCLAW_PROXY_PORT=18788

# OpenClaw gateway URL (proxy forwards allowed requests here)
OPENCLAW_GATEWAY_URL=ws://127.0.0.1:18789

# ─── LOGGING ─────────────────────────────────────────────────
LOG_LEVEL=info
