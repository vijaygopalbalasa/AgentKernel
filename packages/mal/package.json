{
  "name": "@agent-os/mal",
  "version": "0.1.0",
  "type": "module",
  "exports": { ".": "./dist/index.js" },
  "types": "./dist/index.d.ts",
  "dependencies": {
    "@agent-os/shared": "workspace:*",
    "@agent-os/kernel": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  },
  "scripts": {
    "build": "tsup src/index.ts src/rate-limiter.ts src/retry.ts src/token-tracker.ts src/streaming.ts src/router.ts --format esm --dts",
    "test": "vitest run"
  }
}
