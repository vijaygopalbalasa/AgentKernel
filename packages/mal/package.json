{
  "name": "@agent-os/mal",
  "version": "0.1.0",
  "type": "module",
  "description": "Model Abstraction Layer for Agent OS — provider routing, rate limiting, token tracking",
  "license": "MIT",
  "exports": { ".": "./dist/index.js" },
  "types": "./dist/index.d.ts",
  "engines": { "node": ">=22.0.0" },
  "dependencies": {
    "@agent-os/shared": "workspace:*",
    "@agent-os/kernel": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  },
  "scripts": {
    "build": "tsup src/index.ts src/rate-limiter.ts src/retry.ts src/token-tracker.ts src/streaming.ts src/router.ts --format esm --dts",
    "test": "vitest run"
  }
}
