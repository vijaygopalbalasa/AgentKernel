{
  "name": "@agentkernel/runtime",
  "version": "0.1.0",
  "type": "module",
  "description": "Agent runtime for AgentKernel — lifecycle, sandboxing, scheduling, state machines",
  "license": "MIT",
  "exports": { ".": "./dist/index.js" },
  "types": "./dist/index.d.ts",
  "engines": { "node": ">=20.0.0" },
  "dependencies": {
    "@agentkernel/shared": "workspace:*",
    "@agentkernel/kernel": "workspace:*"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "test": "vitest run"
  }
}
