{
  "name": "@agentrun/runtime",
  "version": "0.1.0",
  "type": "module",
  "description": "Agent runtime for AgentRun — lifecycle, sandboxing, scheduling, state machines",
  "license": "MIT",
  "exports": { ".": "./dist/index.js" },
  "types": "./dist/index.d.ts",
  "engines": { "node": ">=20.0.0" },
  "dependencies": {
    "@agentrun/shared": "workspace:*",
    "@agentrun/kernel": "workspace:*"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "test": "vitest run"
  }
}
