{
  "name": "@agent-os/runtime",
  "version": "0.1.0",
  "type": "module",
  "description": "Agent runtime for Agent OS — lifecycle, sandboxing, scheduling, state machines",
  "license": "MIT",
  "exports": { ".": "./dist/index.js" },
  "types": "./dist/index.d.ts",
  "engines": { "node": ">=22.0.0" },
  "dependencies": {
    "@agent-os/shared": "workspace:*",
    "@agent-os/kernel": "workspace:*"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "test": "vitest run"
  }
}
