# AgentRun — Minimal Configuration
# Copy to .env and fill in the required values marked with REQUIRED

# ─── REQUIRED ─────────────────────────────────────────────
# At least one LLM provider API key
ANTHROPIC_API_KEY=sk-ant-...

# Security (MUST be ≥32 characters each, use: openssl rand -hex 32)
GATEWAY_AUTH_TOKEN=
PERMISSION_SECRET=
INTERNAL_AUTH_TOKEN=

# ─── OPTIONAL (defaults shown) ────────────────────────────
# DATABASE_URL=postgresql://localhost:5432/agent_os
# REDIS_URL=redis://localhost:6379
# QDRANT_URL=http://localhost:6333
# GATEWAY_PORT=18800
# LOG_LEVEL=info
# REQUIRE_PERSISTENT_STORE=true
# REQUIRE_VECTOR_STORE=false
