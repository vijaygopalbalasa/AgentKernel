{
  "name": "@agentkernel/provider-anthropic",
  "version": "0.1.0",
  "type": "module",
  "description": "Anthropic Claude provider adapter for AgentKernel",
  "license": "MIT",
  "exports": { ".": "./dist/index.js" },
  "types": "./dist/index.d.ts",
  "engines": { "node": ">=20.0.0" },
  "dependencies": {
    "@agentkernel/shared": "workspace:*",
    "@agentkernel/mal": "workspace:*",
    "@anthropic-ai/sdk": "^0.71.2"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "test": "vitest run --exclude '**/*.integration.test.ts'",
    "test:integration": "vitest run --include '**/*.integration.test.ts'"
  }
}
