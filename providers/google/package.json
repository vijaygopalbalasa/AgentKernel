{
  "name": "@agent-os/provider-google",
  "version": "0.1.0",
  "type": "module",
  "description": "Google Gemini provider adapter for Agent OS",
  "license": "MIT",
  "exports": { ".": "./dist/index.js" },
  "types": "./dist/index.d.ts",
  "engines": { "node": ">=22.0.0" },
  "dependencies": {
    "@agent-os/shared": "workspace:*",
    "@agent-os/mal": "workspace:*",
    "@google/genai": "^1.37.0"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "test": "vitest run --passWithNoTests"
  }
}
